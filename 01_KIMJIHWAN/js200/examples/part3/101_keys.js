// 객체에서 키만 추출하기(keys)
const obj = {
    movie: 'Sunny',
    music: 'Like Sugar',
    style: 'Retro',
    price: Infinity
};

// Object.keys()를 활용한 키들만 추출하기
const arr = Object.keys(obj);

console.log(arr);
