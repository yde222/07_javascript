// 객체에서 값만 추출하기(values)
const obj = {
    movie: 'Sunny',
    music: 'Like Sugar',
    style: 'Retro',
    price: Infinity
};

// key와 반대로 Object.values(); 값만 추출하기
const arr = Object.values(obj);
console.log(arr);

