// 진수 변환하기(toString)
const dec = 531;

// Number객체, 숫자 데이터 타입에서만 적용
// 숫자 2를 인자로 넣으면 2진수로 변환
const binByDex = dec.toString(2);
const octByDex = dec.toString(8);
const hexByDex = dec.toString(16);

console.log(binByDex);
console.log(octByDex);
console.log(hexByDex);


